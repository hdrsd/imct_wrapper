#include "pch.h"

#include "imct_wrapper.h"

namespace imctWrapper {
	int	 IMCT_ETH_INITIALIZE(int& nIndex, char* address, int port) {
		return IMCT_ETH_INITIALIZE(nIndex, address, port);
	}
	int	 IMCT_ETH_UNINITIALIZE(int nIndex) {
		return IMCT_ETH_UNINITIALIZE(nIndex);
	}
}

